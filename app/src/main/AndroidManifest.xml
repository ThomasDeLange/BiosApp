<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thomas.biosapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Controllers.MainActivity">
        </activity>
        <activity android:name=".Controllers.DetailedActivity" />
        <activity android:name=".Controllers.PaymentMethodActivity" />
        <activity android:name=".Controllers.SeatsActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />


            </intent-filter>
        </activity>
         <activity android:name=".Controllers.TicketsActivity" />
        <activity android:name=".Controllers.PaymentFinishedActivity" />
        <activity android:name=".Controllers.PaymentProgressActivity" />
        <activity android:name=".Controllers.TicketViewActivity"></activity>
        <activity android:name=".Controllers.DetailedActivity" />
        <activity android:name=".Controllers.BuyTicketActivity" />
        <activity android:name=".Controllers.TicketBuyerInfoActivity" />
        <activity android:name=".Controllers.TicketSeatInfoActivity" />
        <activity android:name=".Controllers.TicketActivity" />
        <activity android:name=".Controllers.TicketDetailActivity"></activity>
    </application>

</manifest>